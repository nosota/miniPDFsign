/// Constants for the image sidebar panel.
abstract final class SidebarConstants {
  /// Default sidebar width in logical pixels.
  static const double defaultWidth = 200.0;

  /// Minimum sidebar width when resizing.
  static const double minWidth = 150.0;

  /// Maximum sidebar width when resizing.
  static const double maxWidth = 400.0;

  /// Height of the sidebar header.
  static const double headerHeight = 44.0;

  /// Height of the add button area at bottom.
  static const double addButtonHeight = 48.0;

  /// Width of the resize handle.
  static const double resizeHandleWidth = 4.0;

  /// Padding around thumbnail images.
  static const double thumbnailPadding = 8.0;

  /// Border radius for thumbnail cards.
  static const double thumbnailBorderRadius = 6.0;

  /// Border width for selected thumbnail.
  static const double selectionBorderWidth = 2.0;

  /// Size of the delete button on hover.
  static const double deleteButtonSize = 24.0;
}
