# PDFSign

macOS-приложение для просмотра PDF-документов и размещения на них изображений (штампов, подписей, печатей).

## Возможности

- Просмотр PDF-документов с непрерывной прокруткой
- Размещение изображений (штампов, подписей) на страницах PDF
- Многооконный режим — каждый документ открывается в отдельном окне
- Библиотека изображений в боковой панели с синхронизацией между окнами
- Комментарии к изображениям
- Поддержка 50+ языков интерфейса, включая RTL (арабский, иврит, персидский)
- Полная поддержка нативного меню macOS

---

## Интерфейс

### Экран приветствия (Welcome Screen)

Главное окно приложения при запуске:

- **Иконка приложения** в центре
- **Кнопка "Open PDF"** — открывает диалог выбора файла
- **Список недавних файлов** — до 12 последних открытых PDF
  - Клик по файлу открывает его в новом окне
  - Наведение показывает кнопку удаления из списка

При открытии PDF экран приветствия автоматически скрывается. При закрытии всех PDF-окон снова появляется.

### Окно просмотра PDF (PDF Viewer)

Основное рабочее окно:

```
┌─────────────────────────────────────────────────────────────────┐
│  [Toolbar: Share button]                                        │
├──────────────────────────────────────────────────┬──────────────┤
│                                                  │              │
│                                                  │   Sidebar    │
│               PDF Document                       │   (Images)   │
│               (continuous scroll)                │              │
│                                                  │              │
│         [Page 3 of 10] — page indicator          │              │
│                                                  │              │
├──────────────────────────────────────────────────┤              │
│  [−] [100%  ▼] [+]  — zoom controls              │              │
└──────────────────────────────────────────────────┴──────────────┘
```

**Компоненты:**

| Компонент | Описание |
|-----------|----------|
| Toolbar | Нативный тулбар macOS с кнопкой Share |
| PDF Area | Область просмотра PDF с непрерывной прокруткой |
| Page Indicator | Плавающий индикатор "Page X of Y", появляется при прокрутке |
| Zoom Controls | Кнопки −/+, выпадающий список масштаба |
| Sidebar | Боковая панель с библиотекой изображений |

### Окно настроек (Settings)

Фиксированный размер 650×500, не изменяется.

**Разделы:**

1. **Language (Язык)**
   - Поле поиска для фильтрации списка
   - "System Default" — использовать язык системы
   - Список всех поддерживаемых языков

2. **Units (Единицы измерения)**
   - Сантиметры (cm) — по умолчанию
   - Дюймы (inch)

---

## Структура меню

### Окно просмотра PDF

```
PDFSign
├── Settings...         ⌘,
├── ───────────────────
└── Quit PDFSign        ⌘Q

File
├── Open...             ⌘O
├── Open Recent         ▸
│   ├── file1.pdf
│   ├── file2.pdf
│   ├── ...
│   ├── ───────────────
│   └── Clear Menu
├── ───────────────────
├── Save                ⌘S
├── Save As...          ⇧⌘S
├── Save All            ⌥⌘S
├── ───────────────────
├── Share               (opens native share picker)
├── ───────────────────
├── Close               ⌘W
└── Close All           ⌥⌘W

Window
├── Minimize            ⌘M
├── Zoom
├── ───────────────────
├── Bring All to Front
├── ───────────────────
├── ✓ Report.pdf        (active window)
├──   Contract.pdf
└──   PDFSign           (Welcome, if visible)
```

### Экран приветствия / Настройки

```
PDFSign
├── Settings...         ⌘,
├── ───────────────────
└── Quit PDFSign        ⌘Q

File
├── Open...             ⌘O
├── Open Recent         ▸
├── ───────────────────
├── Close               ⌘W
└── Close All           ⌥⌘W    (активно если есть PDF-окна)

Window
├── ...
```

> В окнах Welcome и Settings отсутствуют пункты Save, Save As, Save All, Share.

---

## Горячие клавиши

### Управление файлами

| Действие | Сочетание |
|----------|-----------|
| Открыть файл | ⌘O |
| Сохранить | ⌘S |
| Сохранить как | ⇧⌘S |
| Сохранить все | ⌥⌘S |
| Закрыть окно | ⌘W |
| Закрыть все | ⌥⌘W |
| Перезагрузить документ | ⌘R |
| Настройки | ⌘, |
| Выход | ⌘Q |

### Навигация по документу

| Действие | Сочетание |
|----------|-----------|
| Перейти к странице | ⌘G |
| Первая страница | Home |
| Последняя страница | End |
| Предыдущая страница | Page Up |
| Следующая страница | Page Down |
| Прокрутка | ↑ ↓ ← → (по 50 px) |

### Управление масштабом

| Действие | Сочетание |
|----------|-----------|
| Увеличить | ⌘+ |
| Уменьшить | ⌘− |
| Zoom колесом мыши | Ctrl+колесо или ⌘+колесо |
| Zoom на трекпаде | Pinch (щипок двумя пальцами) |

### Работа с изображениями

| Действие | Сочетание |
|----------|-----------|
| Копировать выделенное | ⌘C |
| Вставить (дублировать) | ⌘V |
| Удалить выделенное | Delete или Backspace |

### Управление окнами

| Действие | Сочетание |
|----------|-----------|
| Свернуть | ⌘M |
| Переключение между окнами | ⌘` (системное) |

---

## Масштабирование (Zoom)

### Доступные уровни

| Уровень | Масштаб |
|---------|---------|
| Fit Width | Подогнать по ширине окна |
| 50% | 0.5x |
| 75% | 0.75x |
| 100% | 1.0x (оригинал) |
| 125% | 1.25x |
| 150% | 1.5x |
| 200% | 2.0x |
| 300% | 3.0x |
| 400% | 4.0x |
| 500% | 5.0x |

**Ограничения:** минимум 10%, максимум 500%.

### Элементы управления

- **Кнопка "−"** — уменьшить масштаб (⌘−)
- **Выпадающий список** — выбор из пресетов
- **Кнопка "+"** — увеличить масштаб (⌘+)

При выборе "Fit Width" документ автоматически подстраивается под ширину области просмотра.

### Жесты масштабирования

**Колесо мыши:**
- Ctrl+колесо или ⌘+колесо — zoom in/out
- Масштабирование относительно позиции курсора

**Трекпад (Pinch):**
- Щипок двумя пальцами — zoom in/out
- Центрирование на точке касания (focal point)
- Плавная анимация через Transform.scale

### Технические детали рендеринга

| Параметр | Значение |
|----------|----------|
| Библиотека | **pdfx** (для просмотра), **Syncfusion PDF** (для сохранения) |
| Формат рендеринга | PNG |
| Качество при масштабировании | `FilterQuality.high` — плавная интерполяция |
| Ленивая загрузка | Да — только видимые страницы + 2 страницы буфера |
| Кэширование | LRU-кэш на 10 страниц, с учётом масштаба |
| Диапазон рендеринга | 0.25x – 3.0x (внутренний), UI показывает 10% – 500% |

**Как работает ленивая загрузка:**
- Рендерятся только видимые страницы плюс 2 страницы до и после (буфер)
- При прокрутке страницы вне видимой области отменяют рендеринг
- Кэш хранит до 10 отрендеренных страниц (LRU — вытесняются наименее используемые)
- Каждый уровень масштаба кэшируется отдельно

---

## Индикатор страницы

Плавающий виджет в центре области просмотра:

- Показывает: "Page X of Y"
- Появляется при прокрутке или смене страницы
- Автоматически скрывается через несколько секунд после остановки

---

## Боковая панель (Sidebar)

### Характеристики

| Параметр | Значение |
|----------|----------|
| Ширина по умолчанию | 200 px |
| Минимальная ширина | 150 px |
| Максимальная ширина | 400 px |
| Высота кнопки Add | 48 px |
| Изменение ширины | Перетаскивание левой границы (4 px) |

### Способы добавления изображений

| Способ | Описание |
|--------|----------|
| **Кнопка "Add"** | Внизу панели, открывает системный диалог выбора файлов (множественный выбор) |
| **Drag & Drop** | Перетащить файлы из Finder на панель |
| **Paste (⌘V)** | Вставить изображение из буфера обмена (PNG или JPEG) |
| **Из Photos** | Через диалог выбора файлов доступна Медиатека Фото macOS |

**Источники изображений:**
- Файлы из Finder
- Приложение Фото (Photos) — через диалог "Add" или drag & drop из Photos
- Буфер обмена — скопированные изображения из любых приложений (⌘V)
- Скриншоты — автоматически копируются в буфер (⌘⇧⌃4)

**Поддерживаемые форматы:** PNG, JPG, JPEG, GIF, WebP, BMP, TIFF, TIF

При перетаскивании файлов панель подсвечивается синей рамкой (2 px).

### Пустое состояние

Когда библиотека пуста, отображается:
- Иконка `add_photo_alternate_outlined` (48 px)
- Текст "Drop images here"
- Подсказка "or paste / click Add"

### Структура карточки изображения

```
┌────┬─────────────────────────┐
│ ⋮⋮ │  ┌───────────────────┐  │
│    │  │                   │  │
│grip│  │   🖼 thumbnail     │  │
│    │  │               [×] │ ← кнопка удаления (при наведении)
│    │  └───────────────────┘  │
│    │  Comment text...        │ ← поле комментария
└────┴─────────────────────────┘
```

| Элемент | Описание |
|---------|----------|
| **Grip (⋮⋮)** | Ручка для переупорядочивания, ширина 28 px, курсор grab |
| **Thumbnail** | Превью с сохранением пропорций, border-radius 6 px |
| **Кнопка [×]** | Удаление, появляется при наведении/выделении, 24×24 px |
| **Комментарий** | Текстовое поле высотой 20 px под превью |

### Внешний вид превью

| Состояние | Описание |
|-----------|----------|
| Обычное | Светлая рамка 1 px (dividerColor) |
| Выделенное | Синяя рамка 2 px (primary, 50% прозрачности) |
| При наведении на grip | Фон становится светлее, иконка ярче |

**Кнопка удаления:**
- Позиция: верхний правый угол (4 px отступ)
- Размер: 24×24 px
- Фон: чёрный с 60% прозрачностью
- Иконка: белый крестик 16 px

**Если файл не найден:** показывается иконка `broken_image_outlined` на сером фоне.

### Переупорядочивание

- Потянуть за ручку grip (⋮⋮) — перетащить карточку вверх/вниз
- При перетаскивании: карточка увеличивается на 2%, прозрачность 90%
- Порядок сохраняется в базе данных Isar

### Комментарии к изображениям

**Режим просмотра:**
- Отображает текст комментария (обрезается с `...`)
- Tooltip с полным текстом при наведении (через 500 мс)
- При наведении появляется подчёркивание
- Клик переводит в режим редактирования

**Режим редактирования:**
- Inline TextField, высота 20 px
- Placeholder: "Comment"
- **Enter** — сохранить и выйти
- **Escape** — отменить без сохранения
- Клик вне поля — сохранить и выйти

### Перетаскивание на PDF

**Drag от превью** (не от grip):
1. Потянуть превью изображения на область PDF
2. При перетаскивании оригинал становится полупрозрачным (30%)
3. Feedback: изображение максимум 150 px по большей стороне
4. Отпустить на нужной странице PDF

### Удаление изображения из библиотеки

| Способ | Действие |
|--------|----------|
| Кнопка [×] | Клик по кнопке при наведении |
| Delete/Backspace | При выделенном изображении в sidebar |

### Ограничения изображений

| Параметр | Значение |
|----------|----------|
| Максимальный размер файла | 100 MB |
| Максимальное разрешение | 4096×4096 px |

### Глобальная библиотека

Библиотека изображений **глобальная** — одна на всё приложение, не привязана к конкретному PDF.

- Хранится в **базе данных Isar** (файл `pdfsign.isar` в Documents)
- Синхронизируется между всеми открытыми окнами в реальном времени через `stream.watch()`
- Изменения в одном окне мгновенно отображаются во всех остальных

### Хранение файлов

При добавлении в библиотеку изображение **копируется** в специальную директорию приложения:

```
~/Library/Application Support/com.ivanvaganov.pdfsign/images/
```

**Особенности:**
- Файлы переименовываются с UUID для избежания конфликтов (например, `a1b2c3d4-e5f6-7890-abcd-ef1234567890.png`)
- Оригинальный файл можно безопасно удалить из Finder после добавления
- В базе Isar хранится путь к копии (не к оригиналу)
- При удалении из библиотеки (sidebar) удаляется и запись в Isar, и скопированный файл

---

## Работа с размещёнными изображениями

### Хранение позиций

Позиции размещённых изображений хранятся **в памяти** (Riverpod state), не в файле:

| Что хранится | Где |
|--------------|-----|
| Библиотека изображений | Isar (синхронизируется между окнами) |
| Размещённые изображения | Память окна (теряется при закрытии) |
| Изображения после Save | Встроены в PDF навсегда |

**Важно:** Если закрыть окно без сохранения — все размещённые изображения потеряются.

### Несколько размещений одного изображения

Одно изображение из библиотеки можно разместить **многократно**:
- На одной странице несколько раз
- На разных страницах документа
- Каждое размещение независимо (своя позиция, размер, поворот)

### Привязка к странице

Каждое размещённое изображение привязано к **конкретной странице**:
- Хранится: номер страницы (`pageIndex`) + координаты относительно левого верхнего угла страницы (в PDF points)
- При прокрутке изображение прокручивается вместе со своей страницей
- Нельзя "перетащить" изображение на другую страницу — только удалить и разместить заново

### Границы страницы

**При размещении (drop):**
- Изображение автоматически ограничивается границами страницы (clamping)
- Центр изображения попадает в точку drop, но само изображение не выходит за края

**После размещения:**
- Изображение можно **перетащить за границы** страницы
- Изображение можно **растянуть за пределы** страницы
- При сохранении PDF — части за границами **сохраняются** (нет обрезки)

### PDF с разными размерами страниц

Приложение корректно работает с PDF, где страницы имеют разные размеры:
- Каждая страница рендерится со своими размерами
- Контентная область адаптируется под самую широкую страницу
- При размещении изображения учитываются размеры конкретной страницы

### Удаление из библиотеки vs с PDF

| Действие | Результат |
|----------|-----------|
| Удалить из библиотеки (sidebar) | Удаляется запись из Isar + скопированный файл из Application Support. Уже размещённые на PDF экземпляры остаются и работают (данные в памяти). |
| Удалить с PDF (Delete/Backspace) | Удаляется только конкретное размещение. В библиотеке изображение остаётся. |
| Удалить оригинал в Finder | Ничего не сломается — приложение использует копию из Application Support. |

### Выделение

Клик по изображению выделяет его.

**Внешний вид выделенного объекта:**

```
         ↻              ↻            ← Зоны вращения (снаружи углов)
           ┌──■──────■──┐
           │            │
           ■            ■            ← Боковые маркеры
           │            │
           └──■──────■──┘
         ↻              ↻
```

| Элемент | Описание |
|---------|----------|
| Рамка выделения | Синяя (#0066FF), толщина 2 px, прозрачность 50% |
| Угловые маркеры | Белые квадраты 10×10 px с синей рамкой 2 px |
| Боковые маркеры | Белые квадраты 10×10 px с синей рамкой 2 px |
| Зоны вращения | Невидимые области 20×20 px снаружи углов (отступ 4 px) |

**Эффект наведения:**
- При наведении на маркер его рамка становится ярче (прозрачность с 50% до 100%)
- При наведении на зону вращения появляется иконка изогнутой стрелки (дуга 45° с двумя наконечниками)

### Маркеры изменения размера

**Угловые маркеры (4 шт.):**
- Расположение: углы объекта (topLeft, topRight, bottomLeft, bottomRight)
- Визуальный размер: 10×10 px
- Область клика: 24×24 px (больше для удобства)
- Действие: **пропорциональное** изменение размера (соотношение сторон сохраняется)
- Курсор: диагональная стрелка, меняется в зависимости от угла поворота объекта

**Боковые маркеры (4 шт.):**
- Расположение: середины сторон объекта (top, bottom, left, right)
- Визуальный размер: 10×10 px
- Область клика: 20×20 px
- Действие: **непропорциональное** растягивание (только одна ось)
- Курсор: горизонтальная или вертикальная стрелка, зависит от поворота

**Минимальный размер объекта:** 20×20 px.

### Вращение

**Зоны вращения (4 шт.):**
- Расположение: снаружи каждого угла, по диагонали от объекта
- Отступ от угла: 4 px
- Размер зоны: 20×20 px
- Курсор: grab → grabbing при перетаскивании

**Иконка вращения:**
- Появляется при наведении на зону вращения
- Дуга 45° с двумя треугольными наконечниками на концах
- Цвет: синий (#0066FF), при наведении становится ярче
- Ориентация иконки поворачивается вместе с объектом

**Механика вращения:**
- Вращение вокруг центра объекта
- Угол определяется по положению курсора относительно центра
- Плавный переход через ±180°

### Перемещение

- Курсор при наведении на объект: grab
- Курсор при перетаскивании: grabbing
- При перетаскивании объект следует за курсором с учётом текущего угла поворота

### Отображение размеров

При выделении объекта показывается метка размеров:
- Позиция: под нижней (визуально) стороной объекта, с отступом 16 px
- Формат: `12.5 × 8.3 cm` или `4.9 × 3.3 inch`
- Единицы измерения берутся из настроек
- Метка поворачивается чтобы оставаться читаемой (не вверх ногами)
- Первое число — ширина вдоль текущей нижней стороны

### Копирование и дублирование

**Копирование (⌘C):**
- Выделите размещённое изображение на PDF
- Нажмите ⌘C — изображение копируется во внутренний буфер
- Это НЕ системный буфер обмена, а внутренний буфер приложения

**Вставка/Дублирование (⌘V):**
- Нажмите ⌘V — создаётся копия изображения
- Копия смещается на 20×20 px вправо-вниз от оригинала
- Сохраняются: размер, угол поворота, страница размещения

### Удаление

- Выделите изображение
- Нажмите **Delete** или **Backspace**
- Если фокус на текстовом поле (например, комментарий) — клавиши работают для редактирования текста

---

## Недавние файлы (Recent Files)

### Характеристики

| Параметр | Значение |
|----------|----------|
| Максимальное количество | 12 файлов |
| Сортировка | По дате последнего открытия (новые сверху) |
| Хранение | SharedPreferences |

### Управление

- **Меню File → Open Recent** — список недавних файлов
- **Clear Menu** — очистить весь список
- **Экран приветствия** — клик по файлу открывает его

При попытке открыть несуществующий файл показывается сообщение об ошибке, файл удаляется из списка.

### Что НЕ восстанавливается при повторном открытии

При открытии файла из недавних (или любым другим способом) документ **всегда** открывается с настройками по умолчанию:

| Параметр | Значение при открытии |
|----------|----------------------|
| Масштаб | Fit Width (подгон по ширине) |
| Страница | Первая (page 1) |
| Позиция скролла | Начало документа |
| Размещённые изображения | Нет (только если сохранены в PDF) |

Позиция скролла и уровень масштаба **не сохраняются** между сессиями.

---

## Многооконный режим

### Типы окон

| Тип | Описание |
|-----|----------|
| Welcome | Главное окно (ID "0"), может быть скрыто |
| PDF Viewer | Отдельное окно для каждого документа |
| Settings | Единственный экземпляр (singleton) |

### Поведение

- Каждый PDF открывается в отдельном окне
- При открытии файла, который уже открыт — фокус на существующее окно
- Настройки — всегда одно окно (повторный вызов фокусирует существующее)
- Закрытие последнего видимого окна завершает приложение

### Меню "Window"

- Показывает список всех открытых окон
- Галочка отмечает активное окно
- Клик переключает на выбранное окно

### Заголовок окна

Заголовок PDF-окна отражает состояние документа:

| Состояние | Заголовок |
|-----------|-----------|
| Только открыт (без изменений) | `Report.pdf` |
| Есть несохранённые изменения | `Report.pdf - Edited` |
| После сохранения | `Report.pdf - Saved` |

Суффикс появляется только после первого изменения документа.

### Что считается "изменением" (Edited)

| Действие | Триггерит "Edited" |
|----------|-------------------|
| Добавить изображение (drag & drop) | Да |
| Дублировать изображение (⌘C/⌘V) | Да |
| Удалить изображение (Delete) | Да |
| Переместить изображение | **Нет** |
| Изменить размер изображения | **Нет** |
| Повернуть изображение | **Нет** |

**Важно:** Перемещение, изменение размера и поворот изображений **не** помечают документ как изменённый. Только добавление, дублирование и удаление.

---

## Диалоги

### Сохранение изменений (Save Changes)

Появляется при закрытии окна с несохранёнными изменениями:

```
┌─────────────────────────────────────────────┐
│  Do you want to save changes to            │
│  "Report.pdf"?                             │
│                                            │
│  [Don't Save]  [Cancel]  [Save]           │
└─────────────────────────────────────────────┘
```

- **Save** — сохранить и закрыть
- **Don't Save** — закрыть без сохранения
- **Cancel** — отменить закрытие

### Закрытие всех (Close All)

Появляется при Close All (⌥⌘W) или Quit (⌘Q), если есть несохранённые документы:

```
┌─────────────────────────────────────────────┐
│  You have 3 documents with unsaved         │
│  changes. Do you want to save them?        │
│                                            │
│  [Don't Save]  [Cancel]  [Save All]        │
└─────────────────────────────────────────────┘
```

- **Save All** — сохранить все и выполнить действие
- **Don't Save** — закрыть все без сохранения
- **Cancel** — отменить

### Ошибка сохранения (Save Failed)

Появляется если Save All не смог сохранить некоторые документы:

```
┌─────────────────────────────────────────────┐
│  Failed to save 2 documents.               │
│  Close anyway?                             │
│                                            │
│  [Cancel]  [Close Anyway]                  │
└─────────────────────────────────────────────┘
```

### Переход к странице (Go to Page)

Появляется по ⌘G:

```
┌─────────────────────────────────────────────┐
│  Go to Page                                │
│  ┌────────────────────────────────────┐    │
│  │ 5                                  │    │
│  └────────────────────────────────────┘    │
│  Enter page number (1-25)                  │
│                                            │
│  [Cancel]  [Go]                           │
└─────────────────────────────────────────────┘
```

- Валидация: страница должна быть от 1 до общего числа страниц
- Текст автоматически выделяется для быстрого ввода

---

## Настройки

### Хранение

Настройки хранятся в **SharedPreferences** macOS:

| Ключ | Тип | Значения | По умолчанию |
|------|-----|----------|--------------|
| `locale_preference` | String | `'system'` или код локали (`'ru'`, `'en_US'`) | `'system'` |
| `size_unit_preference` | String | `'cm'` или `'inch'` | `'cm'` |
| `recent_files` | String (JSON) | Массив объектов RecentFile | `[]` |

### Расположение файла

```
~/Library/Preferences/com.example.pdfsign.plist
```

### Синхронизация между окнами

При изменении настроек (язык, единицы измерения) все открытые окна автоматически обновляются через механизм WindowBroadcast.

---

## Поддерживаемые языки

**50+ языков**, включая:

| Код | Язык | Код | Язык |
|-----|------|-----|------|
| ar | العربية (Arabic) | nl | Nederlands (Dutch) |
| az | Azərbaycan (Azerbaijani) | nb | Norsk (Norwegian) |
| be | Беларуская (Belarusian) | pl | Polski (Polish) |
| bg | Български (Bulgarian) | pt_BR | Português (Brazil) |
| ca | Català (Catalan) | pt_PT | Português (Portugal) |
| cs | Čeština (Czech) | ro | Română (Romanian) |
| da | Dansk (Danish) | ru | Русский (Russian) |
| de | Deutsch (German) | sk | Slovenčina (Slovak) |
| el | Ελληνικά (Greek) | sl | Slovenščina (Slovenian) |
| en_AU | English (Australia) | sq | Shqip (Albanian) |
| en_GB | English (UK) | sr | Српски (Serbian) |
| en_NZ | English (New Zealand) | sv | Svenska (Swedish) |
| en_US | English (US) | ta | தமிழ் (Tamil) |
| es_AR | Español (Argentina) | th | ไทย (Thai) |
| es_ES | Español (Spain) | tr | Türkçe (Turkish) |
| es_MX | Español (Mexico) | uk | Українська (Ukrainian) |
| et | Eesti (Estonian) | uz | O'zbek (Uzbek) |
| eu | Euskara (Basque) | vi | Tiếng Việt (Vietnamese) |
| fa | فارسی (Persian) | zh_CN | 中文 (Simplified) |
| fi | Suomi (Finnish) | zh_TW | 中文 (Traditional) |
| fil | Filipino | he | עברית (Hebrew) |
| fr | Français (French) | hi | हिन्दी (Hindi) |
| hr | Hrvatski (Croatian) | hu | Magyar (Hungarian) |
| hy | Armenian | id | Bahasa Indonesia |
| is | Íslenska (Icelandic) | it | Italiano (Italian) |
| ja | 日本語 (Japanese) | ka | ქართული (Georgian) |
| kk | Қазақ (Kazakh) | km | ខ្មែរ (Khmer) |
| ko | 한국어 (Korean) | lt | Lietuvių (Lithuanian) |
| lv | Latviešu (Latvian) | ms | Bahasa Melayu (Malay) |
| mn | Монгол (Mongolian) | my | မြန်မာ (Burmese) |

**RTL языки** (справа налево): Arabic, Hebrew, Persian.

---

## Рабочие процессы

### Открытие PDF

**Через меню:**
1. File → Open (⌘O)
2. Выберите PDF в диалоге
3. Документ откроется в новом окне

**Через Finder:**
1. Дважды кликните на PDF-файл
2. Или перетащите файл на иконку приложения

**Через недавние:**
1. File → Open Recent → выберите файл
2. Или кликните по файлу на экране приветствия

### Размещение изображения

1. Откройте PDF-документ
2. Добавьте изображение в библиотеку (кнопка "+" или drag-and-drop)
3. Перетащите изображение из библиотеки на нужную страницу PDF
4. Настройте положение, размер и поворот
5. Сохраните документ (⌘S)

### Сохранение документа

**Сохранить (⌘S):**
- Перезаписывает исходный файл
- Если файл ещё не сохранялся — показывает диалог "Save As"

**Сохранить как (⇧⌘S):**
- Всегда показывает диалог выбора имени и места

**Сохранить все (⌥⌘S):**
- Сохраняет все открытые документы с несохранёнными изменениями

**Технические детали сохранения:**
- Изображения внедряются в PDF через библиотеку Syncfusion
- Оригинальный PDF хранится в памяти (для корректного Save без накопления изменений)
- После сохранения изображения остаются редактируемыми в текущей сессии
- Механизм идентичен функции Share (но без временного файла)

### Закрытие с несохранёнными изменениями

При закрытии документа с изменениями появляется диалог:
- **Save** — сохранить и закрыть
- **Don't Save** — закрыть без сохранения
- **Cancel** — отменить закрытие

При закрытии всех (⌥⌘W) или выходе (⌘Q):
- **Save All** — сохранить все и выполнить действие
- **Discard** — закрыть все без сохранения
- **Cancel** — отменить

### Обработка ошибок доступа к файлам

При открытии PDF из защищённых папок (например, Downloads) macOS может запросить разрешение.

**Механизм retry:**
- Если доступ запрещён — приложение автоматически повторяет попытку
- До 20 попыток с интервалом 1.5 секунды (всего ~30 секунд)
- Во время ожидания показывается индикатор загрузки (не ошибка)
- Если разрешение не получено за 30 секунд — окно закрывается

Это даёт пользователю время нажать "Allow" в системном диалоге macOS.

### Отправка документа (Share)

Функция Share в тулбаре или меню File позволяет отправить PDF через системный механизм macOS.

**Алгоритм работы:**

1. **Если изображений не размещено:**
   - Отправляется оригинальный PDF-файл напрямую

2. **Если есть размещённые изображения:**
   - Создаётся временная копия PDF с встроенными изображениями
   - Временный файл сохраняется в системную директорию temp:
     ```
     /var/folders/.../T/{uuid}.pdf
     ```
   - Изображения внедряются в PDF с учётом позиции, размера и поворота
   - Отправляется временный файл
   - Через 5 секунд временный файл автоматически удаляется

**Технические детали:**
- Используется `Share.shareXFiles` из пакета `share_plus`
- Открывается нативный macOS Share picker
- Встраивание изображений выполняется через Syncfusion PDF

---

## Известные ограничения

### Обработка изображений

| Особенность | Статус |
|-------------|--------|
| PNG с прозрачностью (alpha) | В UI отображается корректно. При сохранении в PDF — зависит от библиотеки Syncfusion |
| EXIF-ориентация | **Не обрабатывается** — фото с камеры могут отображаться повёрнутыми |
| Прямой drag из Finder на PDF | **Не поддерживается** — только через библиотеку (sidebar) |

### Редактирование

| Особенность | Статус |
|-------------|--------|
| Множественное выделение | **Нет** — только одно изображение за раз |
| Snap/привязка при перемещении | **Нет** — свободное позиционирование |
| Ввод точных координат/размеров | **Нет** — только отображение размеров (клик переключает cm/inch) |
| Редактирование после Save | **Нет** — сохранённые изображения "запекаются" в PDF |

### EXIF-ориентация

Изображения с EXIF-метаданными ориентации (например, фото с iPhone, сделанные в портретном режиме) **не корректируются автоматически**. Они могут отображаться повёрнутыми на 90°/180°/270°.

**Обходной путь:** перед добавлением в библиотеку откройте изображение в Preview.app и экспортируйте — это применит EXIF-поворот к пикселям.

---

## Планы на будущее

### Undo/Redo (отмена и повтор)

Возможность отменять и повторять действия:
- ⌘Z — отменить последнее действие
- ⇧⌘Z — повторить отменённое действие
- Глубина истории: 50 операций
- Поддержка: перемещение, изменение размера, поворот, удаление

### Z-order (порядок наложения)

Возможность изменять порядок наложения размещённых изображений:
- Переместить на передний план
- Переместить на задний план
- Переместить вверх/вниз на один уровень

### Конвертация изображений в PDF

Возможность создавать PDF из набора изображений:
- Выбор нескольких изображений
- Настройка порядка страниц
- Экспорт в единый PDF-документ

### Множественное выделение

Возможность выделять несколько изображений одновременно:
- Shift+click для добавления к выделению
- Marquee selection (рамка выделения)
- Групповые операции: перемещение, удаление, выравнивание

### Snap/привязка

Автоматическая привязка при перемещении и изменении размера:
- К краям страницы
- К другим объектам
- К сетке (опционально)

### Inspector panel

Панель свойств для точного редактирования:
- Ввод координат X, Y числами
- Ввод ширины и высоты
- Ввод угла поворота

### Обработка EXIF-ориентации

Автоматическое применение EXIF-поворота при импорте изображений.

### Восстановление позиции при открытии

Сохранение и восстановление:
- Позиции скролла
- Уровня масштаба
- Текущей страницы
